name: contributions

# Refreshes the contribution grid in the header once a day. The bot's own commit carries [skip ci] and the
# actor guard below, so a refresh never triggers another refresh.
on:
  schedule:
    - cron: "17 5 * * *"
  workflow_dispatch:
  push:
    branches: [master]

permissions:
  contents: write

jobs:
  refresh:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node tools/fetch-contributions.mjs culinablaz tools/contributions.json
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - run: node tools/gen-header.mjs tools assets
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tools/contributions.json assets/header-*.svg
          git diff --cached --quiet || git commit -m "chore: refresh contribution grid [skip ci]"
          git push
